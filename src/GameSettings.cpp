#include "stdafx.h"
#include "GameSettings.h"

const FPoint GameSettings::cannonPos(GameSettings::cannonPX, GameSettings::cannonPY);

const float GameSettings::gunScale(0.3);
const float GameSettings::bulletScale(0.2);
const float GameSettings::buttonScale(0.6);

const float GameSettings::targetScale1(0.2);
const float GameSettings::targetScale2(0.4);

const float GameSettings::delayOfShoots(0.1);

const float GameSettings::matchTime(20);


GameSettings::GameSettings()
{
}


GameSettings::~GameSettings()
{
}
